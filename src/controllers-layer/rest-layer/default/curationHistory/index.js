const express = require("express");

// CurationHistory Db Object Rest Api Router
const curationHistoryRouter = express.Router();

// add CurationHistory controllers

module.exports = curationHistoryRouter;
